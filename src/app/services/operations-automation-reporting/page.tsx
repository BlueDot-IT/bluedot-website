import { metadataForServicePage, renderServicePage } from '@/components/services/ServiceLandingPage'

export const metadata = metadataForServicePage('operations-automation-reporting')

export default function ServicePage() {
  return renderServicePage('operations-automation-reporting')
}
